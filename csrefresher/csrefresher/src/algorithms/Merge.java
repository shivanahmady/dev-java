package algorithms;

public class Merge {

    // public static void mergesort(int[] arr) {

    // mergesort(arr, 0, arr.length - 1);

    // }

    // public static void mergesort(int[] arr, int ls, int re) {

    // if (ls >= re)
    // return;
    // int middle = (ls + re) / 2;

    // mergesort(arr, ls, middle);
    // mergesort(arr, middle + 1, re);

    // mergeHalves(arr, ls, re);

    // }

    // public static void mergeHalves(int[] arr, int lstart, int rend) {
    // int le = (lstart + rend) / 2;

    // }

    // public static void main(String[] args) {

    // }

}
